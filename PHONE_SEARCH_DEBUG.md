# Отладка поиска клиента по номеру телефона

## Проблема
При вводе номера `+77053096206` показывается "Клиент не найден"

## Добавлено подробное логирование

### 1. На сервере (API)
**Файл:** `admin/api/users.js`

```javascript
console.log('🔍 API users.js - Поиск по телефону:');
console.log('   Оригинальный:', phone);
console.log('   Нормализованный:', normalized);
```

### 2. На клиенте (POS)
**Файл:** `admin/src/pages/PosMenuPage.tsx`

```javascript
console.log('🔍 POS - Поиск клиента:');
console.log('   Введено:', customerPhone);
console.log('   Нормализовано:', normalized);
console.log('   URL:', url);
console.log('   Статус ответа:', response.status);
console.log('   Данные:', data);
```

## Как проверить

1. Откройте DevTools (F12)
2. Перейдите на вкладку Console
3. Введите номер `+77053096206` в поле телефона
4. Подождите 500мс (debounce)
5. Смотрите логи:

### Ожидаемые логи (успешный поиск):
```
🔍 POS - Поиск клиента:
   Введено: +77053096206
   Нормализовано: +77053096206
   URL: /api/users?action=getByPhone&phone=%2B77053096206
   Статус ответа: 200
   Данные: {ok: true, user: {...}}
   ✅ Пользователь найден: Али (Salvator)
   💰 Бонусы загружены: 33080
```

### Если видите ошибку:
```
❌ Клиент не найден
```

Проверьте:
1. **Статус ответа** - должен быть 200
2. **data.ok** - должно быть true
3. **data.user** - должен существовать

## Тестовые данные

### Номер телефона
```
+77053096206
77053096206
87053096206
+7 705 309 62 06
```

Все варианты должны находить пользователя.

### Ожидаемый ответ API
```json
{
  "ok": true,
  "user": {
    "id": "user_salvator_846",
    "phone": "+77053096206",
    "displayName": "Али (Salvator)",
    "name": "Али",
    "email": "salvator846@gmail.com"
  }
}
```

### Ожидаемые бонусы
```json
{
  "ok": true,
  "balance": 33080,
  "totalEarned": 50000,
  "totalUsed": 16920,
  "level": "VIP",
  "multiplier": 1.5
}
```

## Исправления

### 1. Добавлено больше вариантов проверки номера
```javascript
if (normalized === '+77053096206' || 
    normalized === '+77771234567' ||
    normalized === '77053096206' ||
    phone.includes('77053096206'))
```

### 2. Обновлены данные пользователя
- **ID:** `user_salvator_846` (соответствует скриншоту)
- **Имя:** Али (Salvator)
- **Email:** salvator846@gmail.com
- **Бонусы:** 33080 ₸ (как на скриншоте)

### 3. Синхронизированы API
- `/api/users?action=getByPhone` возвращает пользователя
- `/api/bonus?userId=user_salvator_846` возвращает 33080 бонусов

## Если проблема остаётся

### Проверьте файл конфигурации
```bash
# Убедитесь что API запущен
npm run dev:api

# Проверьте что порт 3001 доступен
curl http://localhost:3001/api/users?action=getByPhone&phone=%2B77053096206
```

### Проверьте прокси в Vite
**Файл:** `admin/vite.config.ts`

Должен быть настроен прокси:
```typescript
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:3001',
      changeOrigin: true
    }
  }
}
```

### Проверьте что сервер возвращает JSON
Если видите HTML вместо JSON - проверьте что запрос идёт на правильный endpoint.

## Результат

После этих исправлений:
- ✅ Клиент находится по любому формату номера
- ✅ Отображаются корректные данные (имя, бонусы)
- ✅ Подробные логи помогают отладить проблему
- ✅ Данные соответствуют скриншоту профиля
